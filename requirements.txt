# Dependências do projeto — Plataforma de Agentes
#
# Instalar com: pip install -r requirements.txt

requests==2.32.3

# Painel do Conselho (web interno)
fastapi==0.115.6
uvicorn==0.32.1
jinja2==3.1.4
python-multipart==0.0.12

# LLM Router — modo dry-run funciona sem instalar.
# Instalar quando LLM_MODO=real for ativado:
anthropic>=0.40.0,<1.0.0
python-dotenv==1.0.1
